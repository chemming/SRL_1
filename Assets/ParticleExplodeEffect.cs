using UnityEngine;
using System.Collections;

public class ParticleExplodeEffect : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
		Destroy (this, 0.7f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
